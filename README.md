1. Название и суть проекта

Проект: Blackburn Tools – Devlog / Auto Content Publisher
[![CI](https://github.com/Rudiak-Kirill/blackburn_tools/actions/workflows/ci.yml/badge.svg)](https://github.com/Rudiak-Kirill/blackburn_tools/actions/workflows/ci.yml)

# Blackburn Tools

Набор мини-инструментов и AI-агентов для автоматизации разработки и публикации контента.

Этот репозиторий организован как коллекция самостоятельных тулзов под `tools/` — каждая тулза живёт в своей папке и содержит свой `README`, зависимости и тесты. Текущая основная тулза — `devblog`.

Структура (коротко):

- `tools/` — папки с отдельными тулзами (например, `tools/devblog/`).
- `docs/` — шаблоны и документация для создания новых тулзов.
- `PRIVATE_MODULES.md` — как подключать приватные расширения.
- `AGENTS.md` — правила создания новых тулзов/агентов.

См. `tools/devblog/README.md` для информации по текущему DevBlog инструменту.
Модуль: Автоматическая генерация и публикация контента из Git-коммитов

Идея:
Сервис на Python, который:

Получает коммиты из репозитория (через Webhook или API).

Превращает их в человекочитаемый пост (с помощью шаблонов + AI).

Автоматически публикует:

в Telegram-канал (dev-блог, changelog),

опционально — в файл/БД для последующей публикации на сайте.

2. Цели и результат

Цель MVP:

Дать возможность одному разработчику:

подключить свой Git-репозиторий (MVP: GitHub),

настроить Telegram-канал,

получать автоматические посты о прогрессе разработки в формате dev-блога.

Результат MVP:

Один работающий сервис:

HTTP-endpoint для Webhook от GitHub.

Минимальная панель/конфиг для настройки проекта.

Автопостинг в Telegram по событиям Git.

Лог всех отправленных постов.

3. Пользовательские сценарии (MVP)
3.1. Сценарий «Разработчик» (основной)

Разработчик регистрируется / создаётся вручную (для MVP можно обойтись конфигом .env + YAML).

В панели/конфиге создаёт «Проект»:

указывает:

имя проекта,

тип репозитория (GitHub),

URL репозитория,

токен доступа / настройки Webhook,

Telegram chat_id канала,

шаблон поста (либо дефолт).

В GitHub на репозиторий вешается Webhook → на пуш.

При каждом пуше сервис:

получает список коммитов,

фильтрует по правилам (например: включать только feat:, fix:, chore:),

агрегирует коммиты в один пост,

генерирует текст поста (шаблон или через AI),

отправляет в Telegram-канал.

Разработчик видит в ТГ живую ленту обновлений.

4. Ограничения и рамки MVP

В рамках MVP:

Только GitHub (GitLab/Gitea — позже).

Только Telegram (сайт — через сохранение постов в локальную БД/файл, без CMS-интеграции).

Авторизация примитивная:

либо один «локальный» пользователь,

либо простая таблица пользователей без сложной auth.

Без полноценного UI:

либо простой HTML на FastAPI (форма создания проекта),

либо конфиг в YAML + CLI.

Не делаем в MVP:

Многоязычность в интерфейсе.

Полноценный биллинг/подписки.

Интеграции с другими мессенджерами.

Сложный frontend.

5. Архитектура (в общих чертах)

Компоненты:

API-сервис (FastAPI/Flask)

Endpoint /webhook/github для приёма событий.

Endpoint /health для мониторинга.

В перспективе — простые CRUD-эндпоинты для проектов.

Модуль интеграции с GitHub

Приём Webhook (push).

Валидация сигнатуры Webhook (shared secret).

Парсинг payload (репо, ветка, список коммитов).

Модуль генерации контента

Правила фильтрации коммитов.

Шаблонизация:

режим без AI: простой шаблон из commit messages.

режим с AI: обращение к OpenAI API (chat.completions).

Возможность конфигурировать стиль поста (сухой devlog, более «человечный»).

Модуль Telegram

Отправка сообщений через Bot API (sendMessage).

Обработка ошибок (rate limit, ошибки сети, логирование).

Параметры:

bot_token,

chat_id,

режим: Markdown / plain text.

Хранилище

MVP: SQLite или Postgres.

Таблицы:

users (если нужно),

projects,

telegram_channels,

repo_integrations,

posts (логи отправленных сообщений),

ai_requests (опционально).

Фоновые задачи

Для MVP можно обходиться inline-обработкой.

На следующих этапах:

Celery / RQ / APScheduler,

или Prefect-flow для более сложных задач (например, ежедневные дайджесты).

6. Модель данных (приблизительно)
6.1. Project

id

name — человекочитаемое название (например, “Blackburn Tools Devlog”)

repo_type — "github"

repo_full_name — "owner/repo"

github_webhook_secret

language — "ru" / "en"

ai_enabled — bool

post_mode:

"per_push" — один пост на каждый push,

"daily_digest" — дайджест (на потом).

telegram_chat_id

telegram_bot_token (или глобальный бот на весь сервис)

created_at, updated_at

6.2. CommitEvent

id

project_id

commit_hash

author

message

pushed_at

data_raw (JSON payload от GitHub)

6.3. Post

id

project_id

source — "github"

content — текст отправленного сообщения

status — "success" | "error"

error_message (если была)

created_at

7. Поток обработки Webhook (MVP)

GitHub отправляет POST на /webhook/github.

Сервис:

Проверяет заголовки (тип события X-GitHub-Event == push).

Проверяет подпись X-Hub-Signature-256.

Находит project по repository.full_name и secret.

Из payload вытаскиваем:

repository.full_name,

ref (ветка, например refs/heads/main),

commits (список).

Фильтрация коммитов:

по ветке (например, только main),

по префиксу сообщения (например, включать только feat:, fix:).

Если после фильтрации список пуст — выходим (ничего не публикуем).

Формируем текст для AI:

название проекта,

список коммитов в виде:

- feat: добавил генерацию PDF для аудита,

- fix: исправил падение при пустом URL.

Если ai_enabled:

отправляем запрос к OpenAI:

system: “Ты помощник-редактор. Сформируй короткий пост в Telegram-канал девлога…”

user: описание + список коммитов.

получаем сгенерированный текст.

Если ai_enabled == false:

используем простой шаблон:

🛠 Обновление в проекте {project_name}

{дата}

Изменения:
- {commit_1}
- {commit_2}
...


Отправляем в Telegram через sendMessage.

Сохраняем запись в posts (статус, текст, время).

8. Интеграция с сайтом (MVP-вариант)

На первом этапе без прямой публикации:

Каждый пост, отправленный в Telegram, сохраняем в таблицу posts.

Формируем простой Markdown-рендер:

или храним content_md,

или роут /project/{id}/feed.md, который отдаёт ленту постов в Markdown.

Далее можно:

либо cron-скриптом забирать этот markdown и собирать статический сайт,

либо просто рендерить страницу «Devlog» из БД.

9. Настройки и конфигурация

Глобальный .env:

APP_ENV (dev/prod)

DB_URL

OPENAI_API_KEY (если используем AI)

TELEGRAM_BOT_TOKEN (если один бот на все проекты)

SECRET_KEY (для подписи и сессий)

GITHUB_WEBHOOK_SECRET_DEFAULT (для теста)

На проект (YAML / БД):

Имя проекта.

Репозиторий.

Секрет Webhook.

Фильтры:

ветка,

паттерны сообщений (регулярка).

Язык текста (RU / EN).

Шаблон поста (если без AI).

Флаг ai_enabled.

Chat ID Telegram.

10. Безопасность и приватность

Не хранить исходный код коммитов, только метаданные (хэш, сообщение).

Webhook должен быть защищён:

по HTTPS,

по подписи X-Hub-Signature-256.

Токены:

OPENAI_API_KEY, TELEGRAM_BOT_TOKEN, GITHUB — только в переменных окружения.

Логи:

не пишем в лог чувствительные данные (токены, секреты).

пишем:

факт приёма Webhook,

id проекта,

статус отправки сообщения.

11. Этапы реализации
Этап 1. Базовый каркас

Настроить FastAPI/Flask проект.

Реализовать /health.

Подключить БД (SQLite/Postgres).

---

Open-core структура и приватные модули

Этот репозиторий оформлен в стиле open-core: публичная, основная функциональность и интеграции находятся в корне репозитория (папки `app/`, `core/`, `db/`, `models/`, `schemas/`, `services/`), а приватные/enterprise расширения выносится в отдельные приватные репозитории.

Рекомендованная организация:

- Публичное ядро (в этом репо):
	- `app/` — точка входа, API, маршруты.
	- `core/` — конфигурация, логирование, auth helper'ы.
	- `db/` — работа с БД и сессии.
	- `integrations/` — адаптеры для Telegram, OpenAI и т.д.
	- `services/` — бизнес-логика (commit processing, content generation).

- Приватные / enterprise модули (в отдельном приватном репо):
	- примеры: `enterprise/analytics`, `enterprise/advanced_ai`, `enterprise/custom_publishers`
	- приватные модули не должны попадать в публичный репозиторий; для них используйте submodules/subtrees или частный PyPI (см. `PRIVATE_MODULES.md`).

Файлы, добавленные в репозиторий:

- `LICENSE` — публичная лицензия (MIT) для ядра проекта.
- `SECURITY.md` — политика ответственного раскрытия уязвимостей.
- `.gitignore` — включает правила для игнорирования приватных модулей (`enterprise/`, `private_modules/` и т. д.).
- `PRIVATE_MODULES.md` — инструкция по подключению приватных репозиториев как модулей (submodule, subtree, pip install, private PyPI).

Ссылки и дальнейшие шаги

- Если вы управляете приватными модулями — заведите отдельный репозиторий для каждого крупного расширения и подключайте его описанными способами.
- Для CI: используйте deploy-ключи или machine accounts с минимальными правами.

Если хотите, могу подготовить пример конфигурации CI (GitHub Actions) для безопасной работы с приватными модулями.

Создать миграции для:

projects,

posts.

Этап 2. Webhook + Telegram без AI

Реализовать /webhook/github:

парсинг payload,

маппинг на project.

Реализовать фильтрацию коммитов.

Реализовать простую генерацию текста по шаблону.

Реализовать отправку в Telegram.

Протестировать на тестовом репо.

Этап 3. AI-режим

Подключить OpenAI API.

Написать промпт для генерации devlog-постов.

Добавить флаг ai_enabled на проект.

Логировать время ответа и возможные ошибки.

Этап 4. Мини-панель/конфиг

Минимальный интерфейс (либо HTML, либо CLI) для:

создания/редактирования проекта,

просмотра логов / последних постов.

12. Критерии готовности MVP

Можно создать проект (через конфиг или форму).

GitHub Webhook успешно принимает push-события.

При пуше в нужную ветку:

сервис получает коммиты,

фильтрует по правилам,

формирует и отправляет пост в Telegram.

Ошибки логируются, не валят весь сервис.

Есть способ посмотреть последние посты (в логах или в простом UI).

AI-режим можно включить/выключить для проекта.

13. Telegram

- Канал проекта: https://t.me/blackburn_devblog
- Архитектура: один глобальный бот (параметр `TELEGRAM_BOT_TOKEN` в `.env`) и для каждого проекта — `telegram_chat_id`.
- Если канал публичный, в `telegram_chat_id` можно указать `@blackburn_devblog`.
- Если канал приватный, добавьте бота в администраторы канала и используйте numeric chat_id формата `-100XXXXXXXXXXXX`.
- Проверьте, что бот имеет право публиковать сообщения в канале.

14. Быстрый запуск и тестирование локально

- Подготовка окружения:
	- Скопируйте `.env.example` → `.env` и заполните:
		- `ADMIN_API_KEY` — ключ для админ-API.
		- `TELEGRAM_BOT_TOKEN` — токен бота (если тестируете отправку в Telegram).
		- `DATABASE_URL` — по умолчанию `sqlite:///./blackburn_tools.db`.

- Запуск сервера разработки:
	```powershell
	cd "d:\GitHub\cursor-repos\Blackburn Tools"
	python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
	```

- Быстрые скрипты для тестирования (директория `scripts/`):
	- `create_test_project_sqlite.py` — создаёт тестовый проект прямо в SQLite (не импортирует `app`, подходит при включённом reload).
	- `simulate_webhook_simple.py` — отправляет подписанный `push`-payload на `POST /webhook/github`.
	- `show_db_entries_sqlite.py` — читает последние записи `posts` и `commit_events` из SQLite.

- Пример последовательности теста:
	1) Запустите сервер (см. выше).
	2) Создайте тест-проект: `python scripts/create_test_project_sqlite.py`.
	3) Отправьте симулированный webhook: `python scripts/simulate_webhook_simple.py`.
	4) Просмотрите записи: `python scripts/show_db_entries_sqlite.py`.

15. Примечания по безопасности и деплою

- Не храните токены в репозитории. Используйте `.env` и секреты окружения в продакшне.
- `ADMIN_API_KEY` обязателен для защиты CRUD-операций над проектами.
- Webhook должен работать через HTTPS и проверять `X-Hub-Signature-256`.

16. Дальше

- Можно включить режим AI (`OPENAI_API_KEY`) — добавлю модуль интеграции и промпт по запросу.
- Подготовлю Alembic миграции и `Dockerfile` при необходимости.